import 'package:flutter/material.dart';

Widget buildTaskSection(nameController, Function(String) addTask) {
  return Container(
    decoration: BoxDecoration(color: Colors.white),
    child: Row(
      children: [

      ],
      )

    );
}